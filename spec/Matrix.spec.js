xdescribe('Matrix', () => {

// should be constructed from a two dimensional array

// should get its width
// should get its height

// should be able to get the value at a given (x, y)

// should be able to set the value at a given (x, y)

// should be able to map the matrix through a given function

// should be able to get a string representation

});
